// Math = built in oblect that provides a 
//        collection of properties and methods  

// ex 
mathEx = Math.PI // equals 3.14 without haveing to type it
console.log(mathEx);

let x = 3.21;
let y = 2;
let z;

z = Math.round(x); // rounds up
z = Math.floor(x); // rounds down
z = Math.ceil(x); // rounds up
z = Math.trunc(x); // eleminates any decemals 
z = Math.pow(x, y); // raises to the power
z = Math.sqrt(x); // finds square root
z = Math.log(x); // finds the logarithmic function 
z = Math.sin(x); // finds sin
z = Math.cos(x); // finds cos
z = Math.tan(x); // finds tan
z = Math.abs(x); // finds absolute value, basically finds the positive number 
z = Math.sign(x); // ?
let max = Math.max(x, y) // finds max
let min = Math.min(x, y)// finds min

console.log(z);
console.log(max);
console.log(min);

